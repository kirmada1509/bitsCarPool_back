package notification

import "bitsCarPool_back/internal/models"

func (s *notificationService) CreateNotification(notification *models.Notification) (string, error){
	// go to collection: notifications
	//access from and put in sent
	//access to and put in received
	return "success", nil
}
	